# Parsable - Protocol for types that can be constructed from Text
# Compiler auto-generates try_create (-> Maybe<Me>) and check_create (-> Result<Me>)
namespace Core

protocol Parsable {
    routine Me.__create__!(from: Text) -> Me
}
