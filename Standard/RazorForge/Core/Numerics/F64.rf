module Core

import Collections
# Core F64 type definition - maps to LLVM double

### A 64-bit IEEE 754 binary floating-point value (double precision).
### `F64` is the default floating-point type for general scientific, financial,
### and system-level numeric code in RazorForge. With 53 bits of significand, it
### provides about 15–16 significant decimal digits of precision and can represent
### values up to approximately ±1.8 × 10³⁰⁸.
###
### Arithmetic operations throw {NumericOverflowError} when the result is
### non-finite (infinity or NaN). Use `$truediv_unchecked` for raw IEEE 754
### semantics without the overflow guard.
@llvm("double")
record F64 obeys Numeric, Ordered
  pass

# IEEE 754 double precision constants
preset F64_EPSILON: F64 = 2.220446049250313e-16_f64
preset F64_MIN: F64 = 2.2250738585072014e-308_f64
preset F64_MAX: F64 = 1.7976931348623157e+308_f64
preset F64_ZERO: F64 = 0.0_f64
preset F64_ONE: F64 = 1.0_f64
preset F64_NAN: F64 = nan_f64
preset F64_INFINITY: F64 = inf_f64

# ============================================================================
# Creators
# ============================================================================

### Converts an `S8` to `F64`. The result is exact for all values.
### :param from: The signed 8-bit integer to convert.
### :returns: The corresponding `F64` value.
@inline
routine F64.$create(from: S8) -> F64
  return signed_to_float[S8, F64](from)

### Converts an `S16` to `F64`. The result is exact for all values.
### :param from: The signed 16-bit integer to convert.
### :returns: The corresponding `F64` value.
@inline
routine F64.$create(from: S16) -> F64
  return signed_to_float[S16, F64](from)

### Converts an `S32` to `F64`. The result is exact for all values.
### :param from: The signed 32-bit integer to convert.
### :returns: The corresponding `F64` value.
@inline
routine F64.$create(from: S32) -> F64
  return signed_to_float[S32, F64](from)

### Converts an `S64` to `F64`.
### Values with magnitude larger than 2⁵³ may be rounded to the nearest
### representable double.
### :param from: The signed 64-bit integer to convert.
### :returns: The closest representable `F64` value.
@inline
routine F64.$create(from: S64) -> F64
  return signed_to_float[S64, F64](from)

### Converts an `S128` to `F64`.
### Precision is limited to approximately 53 bits of significand.
### :param from: The signed 128-bit integer to convert.
### :returns: The closest representable `F64` value.
@inline
routine F64.$create(from: S128) -> F64
  return signed_to_float[S128, F64](from)

### Converts a `U8` to `F64`. The result is exact for all values.
### :param from: The unsigned 8-bit integer to convert.
### :returns: The corresponding `F64` value.
@inline
routine F64.$create(from: U8) -> F64
  return unsigned_to_float[U8, F64](from)

### Converts a `U16` to `F64`. The result is exact for all values.
### :param from: The unsigned 16-bit integer to convert.
### :returns: The corresponding `F64` value.
@inline
routine F64.$create(from: U16) -> F64
  return unsigned_to_float[U16, F64](from)

### Converts a `U32` to `F64`. The result is exact for all values.
### :param from: The unsigned 32-bit integer to convert.
### :returns: The corresponding `F64` value.
@inline
routine F64.$create(from: U32) -> F64
  return unsigned_to_float[U32, F64](from)

### Converts a `U64` to `F64`.
### Values larger than 2⁵³ may be rounded to the nearest representable double.
### :param from: The unsigned 64-bit integer to convert.
### :returns: The closest representable `F64` value.
@inline
routine F64.$create(from: U64) -> F64
  return unsigned_to_float[U64, F64](from)

### Converts a `U128` to `F64`.
### Precision is limited to approximately 53 bits of significand.
### :param from: The unsigned 128-bit integer to convert.
### :returns: The closest representable `F64` value.
@inline
routine F64.$create(from: U128) -> F64
  return unsigned_to_float[U128, F64](from)

### Widens a half-precision float to double precision.
### :param from: The `F16` value to widen.
### :returns: An `F64` carrying the same numeric value.
@inline
routine F64.$create(from: F16) -> F64
  return float_extend[F16, F64](from)

### Widens a single-precision float to double precision.
### :param from: The `F32` value to widen.
### :returns: An `F64` carrying the same numeric value.
@inline
routine F64.$create(from: F32) -> F64
  return float_extend[F32, F64](from)

### Returns an existing `F64` unchanged.
### :param from: The double-precision value to forward.
### :returns: `from`.
@inline
routine F64.$create(from: F64) -> F64
  return from

### Narrows a quadruple-precision float to double precision.
### :param from: The `F128` value to convert.
### :returns: The nearest representable `F64` value.
routine F64.$create(from: F128) -> F64
  return from.F64()

### (`F64.$create(from: D32)` is provided by the decimal32 engine — `D32Trans.rf`.)

### Converts a decimal64 value to double-precision binary floating point.
### :param from: The decimal floating-point input value.
### :returns: The closest representable `F64` value.
routine F64.$create(from: D64) -> F64
  return d64_to_f128(x: from).F64()

### Converts a decimal128 value to double-precision binary floating point.
### :param from: The decimal floating-point input value.
### :returns: The closest representable `F64` value.
routine F64.$create(from: D128) -> F64
  return d128_to_f256(x: from).to_f128().F64()

# ============================================================================
# Arithmetic Operations
# ============================================================================

# Checked arithmetic crashes with a SPECIFIC cause when a result is non-finite, mirroring the
# `Decimal` contract: ±inf -> NumericOverflowError, NaN -> NumericDomainError, x/0 -> DivisionByZeroError.
# `@crash_only` makes each crash unconditional, so the wired operators that call them stay
# non-failable — a non-finite result is a bug surfaced loudly at its source, not a recoverable error.
# Opt out with the `$*_unchecked` operators (raw IEEE 754 propagation) or `.finite!()` (recoverable).
@crash_only
routine f64_overflow_crash!() -> F64
  throw NumericOverflowError(description: "F64 overflow: the result is infinite (out of range).")
@crash_only
routine f64_domain_crash!() -> F64
  throw NumericDomainError(description: "F64: undefined operation with no real result (e.g. 0/0 or inf - inf).")
@crash_only
routine f64_divzero_crash!() -> F64
  throw DivisionByZeroError()

### Classifies a non-finite result: NaN -> domain crash, ±infinity -> overflow crash.
@crash_only
routine f64_nonfinite_crash!(r: F64) -> F64
  if r.is_nan()
    return f64_domain_crash!()
  return f64_overflow_crash!()

### Returns `me + you`. Checked: crashes if the IEEE result is ±infinity (overflow) or NaN
### (domain). Use `$add_unchecked` for raw IEEE 754 propagation.
### :param you: The addend.
### :returns: The double-precision sum (always finite).
routine F64.$add(you: F64) -> F64
  var r = float_add[F64](a: me, b: you)
  if r.is_finite()
    return r
  return f64_nonfinite_crash!(r: r)

### Returns `me - you`. Checked: crashes if the IEEE result is non-finite.
### Use `$sub_unchecked` for raw IEEE 754 propagation.
### :param you: The value to subtract.
### :returns: The double-precision difference (always finite).
routine F64.$sub(you: F64) -> F64
  var r = float_sub[F64](a: me, b: you)
  if r.is_finite()
    return r
  return f64_nonfinite_crash!(r: r)

### Returns `me * you`. Checked: crashes if the IEEE result is non-finite.
### Use `$mul_unchecked` for raw IEEE 754 propagation.
### :param you: The multiplier.
### :returns: The double-precision product (always finite).
routine F64.$mul(you: F64) -> F64
  var r = float_mul[F64](a: me, b: you)
  if r.is_finite()
    return r
  return f64_nonfinite_crash!(r: r)

### Returns `me / you`. Checked: `x / 0` crashes with DivisionByZeroError, `0 / 0` with a domain
### error, and an overflow to ±infinity with an overflow error. Use `$truediv_unchecked` for raw
### IEEE 754 (`x/0` -> ±inf, `0/0` -> NaN), or `(a / b).finite!()` for recoverable handling.
### :param you: The divisor.
### :returns: The double-precision quotient (always finite).
routine F64.$truediv(you: F64) -> F64
  var r = float_div[F64](a: me, b: you)
  if r.is_finite()
    return r
  if you.is_zero() and not me.is_zero()
    return f64_divzero_crash!()
  return f64_nonfinite_crash!(r: r)

### Returns `me` if it is finite; throws (RECOVERABLE) if it is ±infinity or NaN. The bridge from
### raw IEEE propagation back to a handled result: run the unchecked operators, then recover, e.g.
### `var x = when (a *~ b +~ c).finite!() { is err: fallback else v: v }`. (The checked `+ - * /`
### crash instead, so a checked `(a * b)` would already have crashed before reaching `finite!`.)
### :returns: `me`, when finite.
### :throws: {NumericOverflowError} if `me` is ±infinity or NaN.
routine F64.finite!() -> F64
  if not me.is_finite()
    throw NumericOverflowError(description: f"F64 value is non-finite: {me}")
  return me

### Returns the arithmetic negation of this value.
### :returns: `-me` in double precision.
routine F64.$neg() -> F64
  return float_neg[F64](me)

# Unchecked variants — raw IEEE 754 propagation (the float analog of integer `+%`/`+^`). inf/NaN
# are valid IEEE values, not memory hazards, so these are plain (NOT `dangerous`) — the operator
# spelling (`+~`/`-~`/`*~`/`/~`) and `_unchecked` name are the marker. Division by zero yields ±inf
# (x != 0) or NaN (0/0), never undefined behaviour.

### Returns `me / you` with raw IEEE 754 semantics: `x/0` -> ±infinity, `0/0` -> NaN. No crash.
### :param you: The divisor.
### :returns: The double-precision quotient, possibly ±infinity or NaN.
routine F64.$truediv_unchecked(you: F64) -> F64
  return float_div[F64](a: me, b: you)

### Returns `me + you` with raw IEEE 754 propagation (may yield ±infinity or NaN). No crash.
routine F64.$add_unchecked(you: F64) -> F64
  return float_add[F64](a: me, b: you)

### Returns `me - you` with raw IEEE 754 propagation (may yield ±infinity or NaN). No crash.
routine F64.$sub_unchecked(you: F64) -> F64
  return float_sub[F64](a: me, b: you)

### Returns `me * you` with raw IEEE 754 propagation (may yield ±infinity or NaN). No crash.
routine F64.$mul_unchecked(you: F64) -> F64
  return float_mul[F64](a: me, b: you)

# ============================================================================
# Comparison Operations (ordered - false if NaN)
# ============================================================================

### Returns `true` if both values are equal under IEEE 754 ordered comparison.
### :param you: The value to compare against.
### :returns: `true` if `me == you`; `false` if either operand is NaN.
routine F64.$eq(you: F64) -> Bool
  return float_eq[F64](a: me, b: you)

### Compares this value with `you` under IEEE 754 ordered comparison.
### :param you: The value to compare against.
### :returns: `ME_SMALL`, `ME_LARGE`, or `SAME`.
routine F64.$cmp(you: F64) -> ComparisonSign
  if float_lt[F64](a: me, b: you)
    return ME_SMALL
  if float_gt[F64](a: me, b: you)
    return ME_LARGE
  return SAME

# ============================================================================
# Math Operations using intrinsics
# ============================================================================

### Returns the square root. Checked: crashes (domain error) if `me` is negative (`sqrt(<0)` -> NaN).
### Use `sqrt_unchecked` for raw IEEE 754 propagation.
### :returns: The double-precision square root (always finite).
routine F64.sqrt() -> F64
  var r = me.sqrt_unchecked()
  if r.is_finite()
    return r
  return f64_nonfinite_crash!(r: r)

### Returns the square root with raw IEEE 754 propagation (`sqrt(<0)` -> NaN).
routine F64.sqrt_unchecked() -> F64
  return sqrt[F64](me)

### Returns the absolute value.
### :returns: `|me|` in double precision.
routine F64.abs() -> F64
  return float_abs[F64](me)

### Rounds this value toward negative infinity.
### :returns: The greatest integral-valued `F64` not larger than `me`.
routine F64.floor() -> F64
  return floor[F64](me)

### Rounds this value toward positive infinity.
### :returns: The smallest integral-valued `F64` not smaller than `me`.
routine F64.ceil() -> F64
  return ceil[F64](me)

### Rounds this value to the nearest integral-valued `F64`.
### :returns: The rounded result in double precision.
routine F64.round() -> F64
  return round[F64](me)

### Discards the fractional part of this value toward zero.
### :returns: The truncated result in double precision.
routine F64.int_truncate() -> F64
  return float_truncate[F64](me)

### Returns the sine of this value, interpreting it as radians.
### :returns: The double-precision sine result.
routine F64.sin() -> F64
  return sin[F64](me)

### Returns the cosine of this value, interpreting it as radians.
### :returns: The double-precision cosine result.
routine F64.cos() -> F64
  return cos[F64](me)

### Returns `e` raised to this value. Checked: crashes (overflow) if the result is ±infinity.
### Use `exp_unchecked` for raw IEEE 754 propagation.
### :returns: The double-precision exponential result (always finite).
routine F64.exp() -> F64
  var r = me.exp_unchecked()
  if r.is_finite()
    return r
  return f64_nonfinite_crash!(r: r)

### Returns `e` raised to this value with raw IEEE 754 propagation (overflow -> +infinity).
routine F64.exp_unchecked() -> F64
  return exp[F64](me)

### Returns the natural logarithm. Checked: crashes on a non-finite result (`log(0)` -> -inf,
### `log(<0)` -> NaN). Use `log_unchecked` for raw IEEE 754 propagation.
### :returns: The double-precision natural-log result (always finite).
routine F64.log() -> F64
  var r = me.log_unchecked()
  if r.is_finite()
    return r
  return f64_nonfinite_crash!(r: r)

### Returns the natural logarithm with raw IEEE 754 propagation (`log(0)` -> -inf, `log(<0)` -> NaN).
routine F64.log_unchecked() -> F64
  return log[F64](me)

### Returns the base-10 logarithm. Checked: crashes on a non-finite result.
### Use `log10_unchecked` for raw IEEE 754 propagation.
### :returns: The double-precision common-log result (always finite).
routine F64.log10() -> F64
  var r = me.log10_unchecked()
  if r.is_finite()
    return r
  return f64_nonfinite_crash!(r: r)

### Returns the base-10 logarithm with raw IEEE 754 propagation.
routine F64.log10_unchecked() -> F64
  return log10[F64](me)

### Raises this value to the power `exp`. Checked: crashes on a non-finite result (overflow -> inf,
### domain error like `(-1)^0.5` -> NaN). Use `pow_unchecked` for raw IEEE 754 propagation.
### NOTE: contrary to mathematical convention (where 0**0 is indeterminate), `0 ** 0 == 1`,
### following the IEEE 754 `pow` definition (which keeps `x ** 0 == 1` for every `x`).
### :param exp: The exponent applied to `me`.
### :returns: `me` raised to `exp` (always finite).
### Raises this value to `exp` with raw IEEE 754 propagation (inf/NaN on overflow/domain).
### :param exp: The exponent applied to `me`.
### :returns: `me` raised to `exp`, possibly ±infinity or NaN.
routine F64.pow_unchecked(exp: F64) -> F64
  return pow[F64](me, exp)

### `**` operator: raises this value to `exp`, checked (crashes on non-finite result).
### NOTE: contrary to mathematical convention, `0 ** 0 == 1` (IEEE 754 `pow`).
### :param exp: The exponent applied to `me`.
### :returns: `me` raised to `exp` (always finite).
routine F64.$pow!(exp: F64) -> F64
  var r = me.pow_unchecked(exp)
  if r.is_finite()
    return r
  return f64_nonfinite_crash!(r: r)

### Returns this value with its sign bit replaced by the sign of `sign`.
### :param sign: The value supplying the sign bit.
### :returns: A value with the magnitude of `me` and the sign of `sign`.
routine F64.copysign(sign: F64) -> F64
  return copysign[F64](me, sign)

# ============================================================================
# Utility Methods
# ============================================================================

### Returns the smaller of this value and `you`.
### :param you: The value to compare against.
### :returns: The lower of the two values under the ordered comparison rules.
routine F64.min(you: F64) -> F64
  return if me <= you then me else you

### Returns the larger of this value and `you`.
### :param you: The value to compare against.
### :returns: The greater of the two values under the ordered comparison rules.
routine F64.max(you: F64) -> F64
  return if me >= you then me else you

### Restricts this value to the closed interval `[min_val, max_val]`.
### :param min_val: The lower bound to enforce.
### :param max_val: The upper bound to enforce.
### :returns: `me` if already in range, otherwise the nearest bound.
@positional
routine F64.clamp(min_val: F64, max_val: F64) -> F64
  return me.min(max_val).max(min_val)

### Returns `true` if this value is strictly positive (greater than zero).
### :returns: `true` if `me > 0.0`.
routine F64.is_positive() -> Bool
  return me > 0.0_f64

### Returns `true` if this value is strictly negative (less than zero).
### :returns: `true` if `me < 0.0`.
routine F64.is_negative() -> Bool
  return me < 0.0_f64

### Returns `true` if this value compares equal to zero.
### :returns: `true` if `me == 0.0`.
routine F64.is_zero() -> Bool
  return me == 0.0_f64

### Returns `true` when this value is neither NaN nor infinity.
### :returns: Whether the value is finite.
routine F64.is_finite() -> Bool
  return not me.is_nan() and not me.is_infinite()

### Returns `true` when this value encodes an IEEE 754 NaN.
### :returns: Whether the bit pattern represents a not-a-number payload.
routine F64.is_nan() -> Bool
  var bits = me.to_bits()
  var exponent = bit_and[U64](a: bits, b: 0x7FF0000000000000u64)
  var mantissa = bit_and[U64](a: bits, b: 0x000FFFFFFFFFFFFF_u64)
  return exponent == 0x7FF0000000000000u64 and mantissa != 0u64

### Returns `true` when this is a SIGNALING NaN (a NaN whose quiet bit is clear). RazorForge
### arithmetic only ever produces quiet NaN, so this holds only for values from raw bits or
### `signaling_nan()`. `is_nan` stays true for both kinds.
### :returns: Whether the value is a signaling NaN.
routine F64.is_signaling() -> Bool
  return me.is_nan() and bit_and[U64](a: me.to_bits(), b: 0x0008000000000000_u64) == 0_u64

### Returns `true` when this value encodes positive or negative infinity.
### :returns: Whether the bit pattern represents an infinity.
routine F64.is_infinite() -> Bool
  var bits = me.to_bits()
  var exponent = bit_and[U64](a: bits, b: 0x7FF0000000000000u64)
  var mantissa = bit_and[U64](a: bits, b: 0x000FFFFFFFFFFFFF_u64)
  return exponent == 0x7FF0000000000000u64 and mantissa == 0u64

### Returns the sign of this value as `-1.0`, `0.0`, or `1.0`.
### :returns: A unit-sign value describing the sign of `me`.
routine F64.signum() -> F64
  return when
    me > 0.0_f64 => 1.0_f64
    me < 0.0_f64 => -1.0_f64
    else => 0.0_f64

### Reinterprets this float as its raw IEEE 754 bit pattern.
### :returns: The underlying `U64` encoding of this value.
routine F64.to_bits() -> U64
  return reinterpret_bits[F64, U64](me)

### Reinterprets a raw IEEE 754 bit pattern as an `F64`.
### :param bits: The exact double-precision encoding to decode.
### :returns: The `F64` value with that bit pattern.
common routine F64.from_bits(bits: U64) -> F64
  return reinterpret_bits[U64, F64](bits)

### Returns a signaling NaN (exponent all-ones, quiet bit clear, nonzero payload). For interop /
### testing; RazorForge arithmetic never produces one, and consuming it in checked arithmetic crashes.
common routine F64.signaling_nan() -> F64
  return F64.from_bits(bits: 0x7FF0000000000001_u64)

# ============================================================================
# Text Representation and Parsing
# ============================================================================

### Returns the textual representation of this value.
### :returns: The resulting `Text` value.
routine F64.$represent() -> Text
  if me.is_signaling()
    return "snan"
  var cstr_result = rf_format_F64(me)
  return Text(from: CStr(from_ptr: cstr_result))

### Returns the textual representation of this value.
### :returns: The resulting `Text` value.
routine F64.$diagnose() -> Text
  return "F64(" + me.$represent() + ")"

### Parses a decimal float literal into an `F64`.
### :param from_text: The text to parse. Accepts standard decimal notation and
###   the special values `nan`, `inf`, and `infinity` (case-insensitive, with
###   optional leading sign).
### :returns: The `F64` value parsed from the input.
### :throws: {InvalidValueError} if the text is not a valid float literal.
routine F64.$create!(from_text: Text) -> F64
  var len = from_text.count()
  if len == 0u64
    throw InvalidValueError(input: from_text, target: "F64")
  var i = 0u64
  var first = from_text[0u64]
  if first == Character(0x002Bu32) or first == Character(0x002Du32)
    i = 1u64
    if i >= len
      throw InvalidValueError(input: from_text, target: "F64")
  var special = false
  var rest = len - i
  if rest >= 3u64
    var c0 = from_text[i].to_lowercase()
    var c1 = from_text[i + 1u64].to_lowercase()
    var c2 = from_text[i + 2u64].to_lowercase()
    if rest == 3u64 and c0 == Character(0x006Eu32) and c1 == Character(0x0061u32) and c2 == Character(0x006Eu32)
      special = true
    if rest == 3u64 and c0 == Character(0x0069u32) and c1 == Character(0x006Eu32) and c2 == Character(0x0066u32)
      special = true
    if not special and rest == 8u64 and c0 == Character(0x0069u32) and c1 == Character(0x006Eu32) and c2 == Character(0x0066u32)
      var c3 = from_text[i + 3u64].to_lowercase()
      var c4 = from_text[i + 4u64].to_lowercase()
      var c5 = from_text[i + 5u64].to_lowercase()
      var c6 = from_text[i + 6u64].to_lowercase()
      var c7 = from_text[i + 7u64].to_lowercase()
      if c3 == Character(0x0069u32) and c4 == Character(0x006Eu32) and c5 == Character(0x0069u32) and c6 == Character(0x0074u32) and c7 == Character(0x0079u32)
        special = true
  if not special
    var has_digit = false
    var has_dot = false
    var has_exp = false
    while i < len
      var ch = from_text[i]
      if ch.is_numeric()
        has_digit = true
      elseif ch == Character(0x002Eu32)
        if has_dot or has_exp
          throw InvalidValueError(input: from_text, target: "F64")
        has_dot = true
      elseif ch == Character(0x0065u32) or ch == Character(0x0045u32)
        if has_exp or not has_digit
          throw InvalidValueError(input: from_text, target: "F64")
        has_exp = true
        has_digit = false
        if i + 1u64 < len
          var next_ch = from_text[i + 1u64]
          if next_ch == Character(0x002Bu32) or next_ch == Character(0x002Du32)
            i = i + 1u64
      else
        throw InvalidValueError(input: from_text, target: "F64")
      i = i + 1u64
    if not has_digit
      throw InvalidValueError(input: from_text, target: "F64")
  var cstr_val = CStr(from: from_text)
  return rf_parse_F64(cstr_val)

# ============================================================================
# Hashing
# ============================================================================

### Returns an xxHash64-based hash of this value, derived from its raw bit pattern.
### :returns: A `U64` hash of the 64-bit float encoding.
routine F64.$fast_hash() -> U64
  return __hash_leaf(v: reinterpret_bits[F64, U64](me))

# ============================================================================
# Byte Representation
# ============================================================================

### Returns the raw bytes of this value in little-endian order.
### :returns: The resulting `Array[Byte, 8]` value.
routine F64.to_bytes_le() -> Array[Byte, 8]
  var raw = reinterpret_bits[F64, U64](me)
  var result = Array[Byte, 8]()
  result = set_byte_at(array: result, index: 0u64, value: int_truncate[U64, U8](raw))
  result = set_byte_at(array: result, index: 1_u64, value: int_truncate[U64, U8](shift_right_unsigned[U64](raw, 8_u64)))
  result = set_byte_at(array: result, index: 2_u64, value: int_truncate[U64, U8](shift_right_unsigned[U64](raw, 16_u64)))
  result = set_byte_at(array: result, index: 3_u64, value: int_truncate[U64, U8](shift_right_unsigned[U64](raw, 24_u64)))
  result = set_byte_at(array: result, index: 4_u64, value: int_truncate[U64, U8](shift_right_unsigned[U64](raw, 32_u64)))
  result = set_byte_at(array: result, index: 5_u64, value: int_truncate[U64, U8](shift_right_unsigned[U64](raw, 40u64)))
  result = set_byte_at(array: result, index: 6_u64, value: int_truncate[U64, U8](shift_right_unsigned[U64](raw, 48_u64)))
  result = set_byte_at(array: result, index: 7_u64, value: int_truncate[U64, U8](shift_right_unsigned[U64](raw, 56_u64)))
  return result

### Returns the raw bytes of this value in big-endian order.
### :returns: The resulting `Array[Byte, 8]` value.
routine F64.to_bytes_be() -> Array[Byte, 8]
  var raw = byte_swap[U64](reinterpret_bits[F64, U64](me))
  var result = Array[Byte, 8]()
  result = set_byte_at(array: result, index: 0u64, value: int_truncate[U64, U8](raw))
  result = set_byte_at(array: result, index: 1_u64, value: int_truncate[U64, U8](shift_right_unsigned[U64](raw, 8_u64)))
  result = set_byte_at(array: result, index: 2_u64, value: int_truncate[U64, U8](shift_right_unsigned[U64](raw, 16_u64)))
  result = set_byte_at(array: result, index: 3_u64, value: int_truncate[U64, U8](shift_right_unsigned[U64](raw, 24_u64)))
  result = set_byte_at(array: result, index: 4_u64, value: int_truncate[U64, U8](shift_right_unsigned[U64](raw, 32_u64)))
  result = set_byte_at(array: result, index: 5_u64, value: int_truncate[U64, U8](shift_right_unsigned[U64](raw, 40u64)))
  result = set_byte_at(array: result, index: 6_u64, value: int_truncate[U64, U8](shift_right_unsigned[U64](raw, 48_u64)))
  result = set_byte_at(array: result, index: 7_u64, value: int_truncate[U64, U8](shift_right_unsigned[U64](raw, 56_u64)))
  return result

### Returns a keyed hash of this value using SipHash-2-4.
### :param k0: The first 64-bit key word.
### :param k1: The second 64-bit key word.
### :returns: The resulting `U64` value.
routine F64.$hash(k0: U64, k1: U64) -> U64
  return __secure_hash_u64(v: reinterpret_bits[F64, U64](me), k0: k0, k1: k1)

# ============================================================================
# Platform libm (<math.h>) doubles via the runtime's UNIQUELY-NAMED rf_f64_*
# passthroughs (native/runtime/math_functions.c). Unique names are essential: a
# bare `atan`/`cbrt`/... (or a method `x.cbrt()`) resolves to UnpackedFloat's
# same-named 0-arg generic wherever the engine is in scope (a resolver quirk).
# erf/erfc/tgamma/lgamma have no rf_f64_ passthrough but also no UnpackedFloat
# twin, so their bare libm symbols bind fine.
# ============================================================================
external("C") routine rf_f64_sin(value: F64) -> F64
external("C") routine rf_f64_cos(value: F64) -> F64
external("C") routine rf_f64_tan(value: F64) -> F64
external("C") routine rf_f64_asin(value: F64) -> F64
external("C") routine rf_f64_acos(value: F64) -> F64
external("C") routine rf_f64_atan(value: F64) -> F64
external("C") routine rf_f64_atan2(y: F64, x: F64) -> F64
external("C") routine rf_f64_sinh(value: F64) -> F64
external("C") routine rf_f64_cosh(value: F64) -> F64
external("C") routine rf_f64_tanh(value: F64) -> F64
external("C") routine rf_f64_asinh(value: F64) -> F64
external("C") routine rf_f64_acosh(value: F64) -> F64
external("C") routine rf_f64_atanh(value: F64) -> F64
external("C") routine rf_f64_sqrt(value: F64) -> F64
external("C") routine rf_f64_cbrt(value: F64) -> F64
external("C") routine rf_f64_exp(value: F64) -> F64
external("C") routine rf_f64_exp2(value: F64) -> F64
external("C") routine rf_f64_expm1(value: F64) -> F64
external("C") routine rf_f64_log(value: F64) -> F64
external("C") routine rf_f64_log2(value: F64) -> F64
external("C") routine rf_f64_log10(value: F64) -> F64
external("C") routine rf_f64_log1p(value: F64) -> F64
external("C") routine rf_f64_pow(base: F64, exp: F64) -> F64
external("C") routine rf_f64_hypot(x: F64, y: F64) -> F64
external("C") routine rf_f64_fma(x: F64, y: F64, z: F64) -> F64
external("C") routine erf(value: F64) -> F64
external("C") routine erfc(value: F64) -> F64
external("C") routine tgamma(value: F64) -> F64
external("C") routine lgamma(value: F64) -> F64

# ============================================================================
# F64 libm methods (the C99 doubles F64 didn't already have via LLVM intrinsics),
# delegating to the rf_f64_* runtime passthroughs / bare libm externals above.
# ============================================================================
### exp(me) - 1, accurate near 0. Checked: crashes on overflow. Use `expm1_unchecked` for raw IEEE.
routine F64.expm1() -> F64
  var r = me.expm1_unchecked()
  if r.is_finite()
    return r
  return f64_nonfinite_crash!(r: r)
### exp(me) - 1 with raw IEEE 754 propagation.
routine F64.expm1_unchecked() -> F64
  return rf_f64_expm1(value: me)
### 2 raised to me. Checked: crashes on overflow. Use `exp2_unchecked` for raw IEEE.
routine F64.exp2() -> F64
  var r = me.exp2_unchecked()
  if r.is_finite()
    return r
  return f64_nonfinite_crash!(r: r)
### 2 raised to me with raw IEEE 754 propagation.
routine F64.exp2_unchecked() -> F64
  return rf_f64_exp2(value: me)
### log(1 + me), accurate near 0. Checked: crashes on a non-finite result. Use `log1p_unchecked` for raw IEEE.
routine F64.log1p() -> F64
  var r = me.log1p_unchecked()
  if r.is_finite()
    return r
  return f64_nonfinite_crash!(r: r)
### log(1 + me) with raw IEEE 754 propagation.
routine F64.log1p_unchecked() -> F64
  return rf_f64_log1p(value: me)
### Base-2 logarithm. Checked: crashes on a non-finite result. Use `log2_unchecked` for raw IEEE.
routine F64.log2() -> F64
  var r = me.log2_unchecked()
  if r.is_finite()
    return r
  return f64_nonfinite_crash!(r: r)
### Base-2 logarithm with raw IEEE 754 propagation.
routine F64.log2_unchecked() -> F64
  return rf_f64_log2(value: me)
### Euclidean distance sqrt(me^2 + you^2). Checked: crashes on overflow. Use `hypot_unchecked` for raw IEEE.
routine F64.hypot(you: F64) -> F64
  var r = me.hypot_unchecked(you)
  if r.is_finite()
    return r
  return f64_nonfinite_crash!(r: r)
### Euclidean distance with raw IEEE 754 propagation (overflow -> +infinity).
routine F64.hypot_unchecked(you: F64) -> F64
  return rf_f64_hypot(x: me, y: you)
### Fused multiply-add me*b + c (single rounding). Checked: crashes on overflow. Use `fma_unchecked` for raw IEEE.
routine F64.fma(b: F64, c: F64) -> F64
  var r = me.fma_unchecked(b: b, c: c)
  if r.is_finite()
    return r
  return f64_nonfinite_crash!(r: r)
### Fused multiply-add with raw IEEE 754 propagation.
routine F64.fma_unchecked(b: F64, c: F64) -> F64
  return rf_f64_fma(x: me, y: b, z: c)
### Cube root.
routine F64.cbrt() -> F64
  return rf_f64_cbrt(value: me)
### Tangent (radians).
routine F64.tan() -> F64
  return rf_f64_tan(value: me)
### Arc sine. Checked: crashes (domain) if `me` is outside [-1, 1]. Use `asin_unchecked` for raw IEEE.
routine F64.asin() -> F64
  var r = me.asin_unchecked()
  if r.is_finite()
    return r
  return f64_nonfinite_crash!(r: r)
### Arc sine with raw IEEE 754 propagation (`|me| > 1` -> NaN).
routine F64.asin_unchecked() -> F64
  return rf_f64_asin(value: me)
### Arc cosine. Checked: crashes (domain) if `me` is outside [-1, 1]. Use `acos_unchecked` for raw IEEE.
routine F64.acos() -> F64
  var r = me.acos_unchecked()
  if r.is_finite()
    return r
  return f64_nonfinite_crash!(r: r)
### Arc cosine with raw IEEE 754 propagation (`|me| > 1` -> NaN).
routine F64.acos_unchecked() -> F64
  return rf_f64_acos(value: me)
### Arc tangent.
routine F64.atan() -> F64
  return rf_f64_atan(value: me)
### Two-argument arc tangent atan2(me, x).
routine F64.atan2(x: F64) -> F64
  return rf_f64_atan2(y: me, x: x)
### Hyperbolic sine. Checked: crashes on overflow. Use `sinh_unchecked` for raw IEEE.
routine F64.sinh() -> F64
  var r = me.sinh_unchecked()
  if r.is_finite()
    return r
  return f64_nonfinite_crash!(r: r)
### Hyperbolic sine with raw IEEE 754 propagation (overflow -> ±infinity).
routine F64.sinh_unchecked() -> F64
  return rf_f64_sinh(value: me)
### Hyperbolic cosine. Checked: crashes on overflow. Use `cosh_unchecked` for raw IEEE.
routine F64.cosh() -> F64
  var r = me.cosh_unchecked()
  if r.is_finite()
    return r
  return f64_nonfinite_crash!(r: r)
### Hyperbolic cosine with raw IEEE 754 propagation (overflow -> +infinity).
routine F64.cosh_unchecked() -> F64
  return rf_f64_cosh(value: me)
### Hyperbolic tangent.
routine F64.tanh() -> F64
  return rf_f64_tanh(value: me)
### Inverse hyperbolic sine.
routine F64.asinh() -> F64
  return rf_f64_asinh(value: me)
### Inverse hyperbolic cosine. Checked: crashes (domain) if `me < 1`. Use `acosh_unchecked` for raw IEEE.
routine F64.acosh() -> F64
  var r = me.acosh_unchecked()
  if r.is_finite()
    return r
  return f64_nonfinite_crash!(r: r)
### Inverse hyperbolic cosine with raw IEEE 754 propagation (`me < 1` -> NaN).
routine F64.acosh_unchecked() -> F64
  return rf_f64_acosh(value: me)
### Inverse hyperbolic tangent. Checked: crashes if `|me| >= 1` (`±1` -> ±inf, `|me| > 1` -> NaN).
### Use `atanh_unchecked` for raw IEEE.
routine F64.atanh() -> F64
  var r = me.atanh_unchecked()
  if r.is_finite()
    return r
  return f64_nonfinite_crash!(r: r)
### Inverse hyperbolic tangent with raw IEEE 754 propagation (`±1` -> ±inf, `|me| > 1` -> NaN).
routine F64.atanh_unchecked() -> F64
  return rf_f64_atanh(value: me)
### Error function.
routine F64.erf() -> F64
  return erf(value: me)
### Complementary error function.
routine F64.erfc() -> F64
  return erfc(value: me)
### Gamma function. Checked: crashes on a non-finite result (overflow or pole). Use `tgamma_unchecked` for raw IEEE.
routine F64.tgamma() -> F64
  var r = me.tgamma_unchecked()
  if r.is_finite()
    return r
  return f64_nonfinite_crash!(r: r)
### Gamma function with raw IEEE 754 propagation (overflow/pole -> ±infinity or NaN).
routine F64.tgamma_unchecked() -> F64
  return tgamma(value: me)
### Natural log of |gamma(me)|.
routine F64.lgamma() -> F64
  return lgamma(value: me)

# ============================================================================
# Extended libm parity: exp10, sinpi/cospi/tanpi, nextafter, scalbn/ldexp,
# frexp, modf, rint, fdim, fmod, remainder, fmax/fmin, trunc, frac, and
# classification (signbit/is_normal/is_subnormal/fpclassify).
# ============================================================================
external("C") routine rf_f64_exp10(value: F64) -> F64
external("C") routine rf_f64_sinpi(value: F64) -> F64
external("C") routine rf_f64_cospi(value: F64) -> F64
external("C") routine rf_f64_tanpi(value: F64) -> F64
external("C") routine rf_f64_nextafter(x: F64, y: F64) -> F64
external("C") routine rf_f64_scalbn(value: F64, n: S64) -> F64
external("C") routine rf_f64_ilogb(value: F64) -> S64
external("C") routine rf_f64_rint(value: F64) -> F64
external("C") routine rf_f64_fdim(x: F64, y: F64) -> F64
external("C") routine rf_f64_fmod(x: F64, y: F64) -> F64
external("C") routine rf_f64_remainder(x: F64, y: F64) -> F64
external("C") routine rf_f64_fmax(x: F64, y: F64) -> F64
external("C") routine rf_f64_fmin(x: F64, y: F64) -> F64

### Base-10 exponential 10**me. Checked: crashes on overflow. Use `exp10_unchecked` for raw IEEE.
routine F64.exp10() -> F64
  var r = me.exp10_unchecked()
  if r.is_finite()
    return r
  return f64_nonfinite_crash!(r: r)
### Base-10 exponential with raw IEEE 754 propagation (overflow -> +infinity).
routine F64.exp10_unchecked() -> F64
  return rf_f64_exp10(value: me)
### sin(pi*me), exact at integers/half-integers.
routine F64.sinpi() -> F64
  return rf_f64_sinpi(value: me)
### cos(pi*me).
routine F64.cospi() -> F64
  return rf_f64_cospi(value: me)
### tan(pi*me).
routine F64.tanpi() -> F64
  return rf_f64_tanpi(value: me)
### Next representable value after me toward you.
routine F64.nextafter(you: F64) -> F64
  return rf_f64_nextafter(x: me, y: you)
### me * 2^n (C scalbn).
routine F64.scalbn(n: S64) -> F64
  return rf_f64_scalbn(value: me, n: n)
### Alias of scalbn (C ldexp).
routine F64.ldexp(n: S64) -> F64
  return rf_f64_scalbn(value: me, n: n)
### Rounds to nearest integer in the current rounding mode (C rint).
routine F64.rint() -> F64
  return rf_f64_rint(value: me)
### Positive difference max(me - you, 0) (C fdim).
routine F64.fdim(you: F64) -> F64
  return rf_f64_fdim(x: me, y: you)
### Floating-point remainder of me/you (C fmod).
routine F64.fmod(you: F64) -> F64
  return rf_f64_fmod(x: me, y: you)
### IEEE remainder of me/you, ties to even (C remainder).
routine F64.remainder(you: F64) -> F64
  return rf_f64_remainder(x: me, y: you)
### Larger of me and you (C fmax).
routine F64.fmax(you: F64) -> F64
  return rf_f64_fmax(x: me, y: you)
### Smaller of me and you (C fmin).
routine F64.fmin(you: F64) -> F64
  return rf_f64_fmin(x: me, y: you)
### Truncates toward zero (C trunc; alias of int_truncate).
routine F64.trunc() -> F64
  return float_truncate[F64](me)
### Fractional part me - trunc(me).
routine F64.frac() -> F64
  return me - float_truncate[F64](me)
### Integer and fractional parts (C modf); both carry me's sign.
routine F64.modf() -> (F64, F64)
  var ip = float_truncate[F64](me)
  return (ip, me - ip)
### Fraction in [0.5,1) and power-of-two exponent (C frexp).
routine F64.frexp() -> (F64, S64)
  if me.is_zero() or me.is_infinite() or me.is_nan()
    return (me, 0_s64)
  var e = add[S64](a: rf_f64_ilogb(value: me), b: 1_s64)
  return (rf_f64_scalbn(value: me, n: sub[S64](a: 0_s64, b: e)), e)
### True if the sign bit is set (C signbit; true for -0 and -NaN).
routine F64.signbit() -> Bool
  return int_ne[U64](a: bit_and[U64](a: me.to_bits(), b: 0x8000000000000000_u64), b: 0_u64)
### True for a subnormal (denormalized, nonzero) value.
routine F64.is_subnormal() -> Bool
  var bits = me.to_bits()
  var exponent = bit_and[U64](a: bits, b: 0x7FF0000000000000_u64)
  var mantissa = bit_and[U64](a: bits, b: 0x000FFFFFFFFFFFFF_u64)
  return exponent == 0_u64 and mantissa != 0_u64
### True for a finite, normal value (nonzero and not subnormal).
routine F64.is_normal() -> Bool
  return me.is_finite() and not me.is_zero() and not me.is_subnormal()
### C fpclassify: 0 NaN, 1 infinite, 2 zero, 3 subnormal, 4 normal.
routine F64.fpclassify() -> FpClass
  if me.is_signaling()
    return SIGNALING_NAN
  if me.is_nan()
    return NAN
  if me.is_infinite()
    return INFINITE
  if me.is_zero()
    return ZERO
  if me.is_subnormal()
    return SUBNORMAL
  return NORMAL

# ============================================================================
# IEEE 754 Tier 1 — total ordering predicates
# ============================================================================

### Sign-magnitude total-order key: negatives become small, non-negatives large, so an
### unsigned `<=` reproduces the IEEE 754 totalOrder ranking (-NaN < -inf < ... < +inf < +NaN).
routine f64_order_key(bits: U64) -> U64
  if int_ne[U64](a: bit_and[U64](a: bits, b: 0x8000000000000000_u64), b: 0_u64)
    return bit_xor[U64](a: bits, b: 0xFFFFFFFFFFFFFFFF_u64)
  return bit_or[U64](a: bits, b: 0x8000000000000000_u64)

### IEEE 754 totalOrder: a total order over all values including signed zeros and NaNs
### (-NaN < -inf < negatives < -0 < +0 < positives < +inf < +NaN). Never crashes.
### :param you: The value to compare against.
### :returns: Whether `me` precedes or equals `you` in the total order.
routine F64.total_order(you: F64) -> Bool
  return f64_order_key(bits: me.to_bits()) <= f64_order_key(bits: you.to_bits())

### IEEE 754 totalOrderMag: `total_order` applied to the magnitudes (sign bit cleared).
### :param you: The value whose magnitude to compare against.
### :returns: Whether `|me|` precedes or equals `|you|` in the total order.
routine F64.total_order_mag(you: F64) -> Bool
  return bit_and[U64](a: me.to_bits(), b: 0x7FFFFFFFFFFFFFFF_u64) <= bit_and[U64](a: you.to_bits(), b: 0x7FFFFFFFFFFFFFFF_u64)

# ============================================================================
# IEEE 754 Tier 2 — stepping, exponent extraction, scaling, selection, rounding
# ============================================================================

### IEEE 754 nextUp: the least value greater than `me` (nextUp(+inf)=+inf, nextUp(NaN)=NaN,
### nextUp(maxFinite)=+inf, nextUp(-0)=smallest positive subnormal). Propagating; never crashes.
### :returns: The next representable value toward +infinity.
routine F64.next_up() -> F64
  return me.nextafter(you: F64_INFINITY)

### IEEE 754 nextDown: the greatest value less than `me` (= -nextUp(-me)). Never crashes.
### :returns: The next representable value toward -infinity.
routine F64.next_down() -> F64
  return me.nextafter(you: -F64_INFINITY)

### IEEE 754 logb: the unbiased exponent of `me` as a floating value (logb(0)=-inf, logb(±inf)=+inf,
### logb(NaN)=NaN; otherwise floor(log2|me|)). Propagating; never crashes.
### :returns: The radix-2 exponent of `me`.
routine F64.logb() -> F64
  if me.is_nan()
    return me
  if me.is_infinite()
    return F64_INFINITY
  if me.is_zero()
    return -F64_INFINITY
  return F64(from: rf_f64_ilogb(value: me))

### IEEE 754 scaleB: `me * 2^n`, computed without intermediate rounding. Checked: crashes if a
### finite `me` overflows to ±infinity (use `scaleb_unchecked` for raw IEEE propagation).
### :param n: The power-of-two exponent to apply.
### :returns: `me` scaled by `2^n`.
routine F64.scaleb(n: S64) -> F64
  var r = rf_f64_scalbn(value: me, n: n)
  if r.is_finite() or not me.is_finite()
    return r
  return f64_nonfinite_crash!(r: r)

### `me * 2^n` with raw IEEE 754 propagation (overflow -> ±infinity).
### :param n: The power-of-two exponent to apply.
### :returns: `me` scaled by `2^n`.
routine F64.scaleb_unchecked(n: S64) -> F64
  return rf_f64_scalbn(value: me, n: n)

### IEEE 754-2008 minNum: the smaller operand, treating NaN as missing (returns the non-NaN
### operand; NaN only if both are NaN). Never crashes.
### :param you: The value to compare against.
### :returns: The minimum of `me` and `you`, ignoring NaN.
routine F64.min_num(you: F64) -> F64
  return rf_f64_fmin(x: me, y: you)

### IEEE 754-2008 maxNum: the larger operand, treating NaN as missing. Never crashes.
### :param you: The value to compare against.
### :returns: The maximum of `me` and `you`, ignoring NaN.
routine F64.max_num(you: F64) -> F64
  return rf_f64_fmax(x: me, y: you)

### IEEE 754-2019 minimum: the smaller operand, propagating NaN, and ordering -0 below +0.
### :param you: The value to compare against.
### :returns: The minimum of `me` and `you`, NaN if either is NaN.
routine F64.minimum(you: F64) -> F64
  if me.is_nan()
    return me
  if you.is_nan()
    return you
  if me == you
    if me.is_zero()
      return if me.signbit() then me else you
    return me
  return if me < you then me else you

### IEEE 754-2019 maximum: the larger operand, propagating NaN, and ordering +0 above -0.
### :param you: The value to compare against.
### :returns: The maximum of `me` and `you`, NaN if either is NaN.
routine F64.maximum(you: F64) -> F64
  if me.is_nan()
    return me
  if you.is_nan()
    return you
  if me == you
    if me.is_zero()
      return if me.signbit() then you else me
    return me
  return if me > you then me else you

### IEEE 754 roundToIntegralTiesToAway: rounds to the nearest integer, halves away from zero
### (round(0.5)=1, round(-2.5)=-3). Exact and never crashes (non-finite inputs pass through).
### :returns: `me` rounded to an integral value with ties away from zero.
routine F64.round_ties_away() -> F64
  if not me.is_finite()
    return me
  var a = me.abs()
  var t = a.trunc()
  var rounded = if a - t >= 0.5_f64 then t + 1.0_f64 else t
  return rounded.copysign(sign: me)
