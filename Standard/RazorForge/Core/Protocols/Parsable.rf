# Parsable - Protocol for types that can be constructed from Text
# Compiler auto-generates create! (-> Maybe[Me]) and check_create (-> Result[Me])
module Core

protocol Parsable
  routine Me.$create!(from: Text) -> Me
