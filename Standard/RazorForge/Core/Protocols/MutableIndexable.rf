module Core

### Integer-indexed read and write access for mutable ordered sequences.
### Extends {Indexable[V]} with a write arm for the `[]` assignment operator.
### :typeparam V: The element type stored in the sequence.
protocol MutableIndexable[V] obeys Indexable[V]
  ### Replaces the element at `index` with `value`.
  ### :param index: The zero-based position to update.
  ### :param value: The replacement value.
  ### :throws: {IndexOutOfBoundsError} if `index >= count()`.
  routine Me.$setitem!(index: U64, value: ?V)
