# Identifiable - Protocol for types that support identity comparison (===, !==)
module Core

protocol Identifiable
  @[readonly, innate]
  routine Me.$same(you: Me) -> Bool

  @[readonly, innate]
  routine Me.$notsame(you: Me) -> Bool
