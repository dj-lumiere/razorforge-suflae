# Test standalone when block

routine start() {
    let a: s32 = 10
    let b: s32 = 0
    let result: s32 = 0

    when {
        b != 0 => result = a / b,
        _ => result = -1
    }

    return result
}
