# Simple loop test
external("C") routine printf(fmt: text, ...) -> s32

routine start() {
    var i: s64 = 0

    # Test loop with break
    loop {
        printf("i = %lld\n", i)
        i = i + 1
        if i > 3 {
            break
        }
    }

    printf("Done! Final i = %lld\n", i)
}
