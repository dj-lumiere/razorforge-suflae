module Tests/Stdlib/TcheckD64Sin

import IO/Console

# Per-function cross-platform softfloat check (split out so CI shows each function's pass/fail).
routine start()
  show((1.0_d64).sin().$represent())
  return
