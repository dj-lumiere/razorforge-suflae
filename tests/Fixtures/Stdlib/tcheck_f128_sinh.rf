module Tests/Stdlib/TcheckF128Sinh

import IO/Console

# Per-function cross-platform softfloat check (split out so CI shows each function's pass/fail).
routine start()
  show((1.0_f128).sinh_unchecked().$represent())
  return
