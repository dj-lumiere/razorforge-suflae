# Test when expression crash

routine main() -> s32 {
    let a: s32 = 10
    let b: s32 = 0

    when {
        b != 0 => Console.print_line(f"Quotient: {a / b}"),
        _ => Console.print_line("Cannot divide by zero!")
    }

    return 0
}
