# Test that throw with string literal is not allowed

routine divide!(a: s32, b: s32) -> s32
{
    if (b == 0)
    {
        # This should fail - cannot throw strings
        throw "Division by zero"
    }
    return a // b
}

routine start()
{
}
