# RazorForge equivalent of our Suflae program
import stdlib/core

routine greet(name: Text) -> Text {
    return f"Hello, {name}!"
}

routine start() {
    let message = greet("World")
    display_line(message)
    raise ProcessComplete()
}
